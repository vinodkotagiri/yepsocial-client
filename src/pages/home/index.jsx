import React, { Fragment } from 'react'
import TopBar from '../../components/topbar'
import { Container } from 'react-bootstrap'
const Home = () => {
	return (
		<Fragment>
			<Container fluid style={{ backgroundColor: '#f8f9fa', height: '100vh' }}>
				Homepage
			</Container>
		</Fragment>
	)
}

export default Home
